# Simple CAT Tool 路线图（更新于 2026-02-14）

> 该文档定位为“中期方向（Now/Next/Later）”，不是实时执行看板。  
> 实时状态请看：`DOCS/CURRENT_STATUS.md`。

## 对旧版路线图的判断

旧版 `v0.3` 清单已部分过时：其中若干“未完成项”在代码中已落地，继续作为执行依据会误导优先级。  
因此本文件已重置为滚动方向文档。

## Now（当前 1-2 个迭代）

1. 保持门禁一致性（已完成一次修复）：新增/变更 `CATDatabase` 公共 API 时，同步更新架构守卫清单。
2. 保持功能开发节奏下的正确性：
- 关键链路改动必须有成功/失败路径测试。
- 触达事务链路时保证无半状态。
3. 建立 lint warning 压降节奏：
- 不追求一次性清零。
- 防止 warning 数继续上升。

## Next（后续迭代）

1. 预翻译流程产品化（TM -> AI -> Draft）与任务可观测性增强。
2. 批量 QA 规则框架（数字/术语/标点）与结果反馈统一。
3. 继续收紧跨层类型边界，减少历史 `any` 残留（重点在边界层与测试桩）。

## Later（中长期）

1. Provider 插件化深化（AI/TM/TB 供应方替换成本继续下降）。
2. 文件格式扩展（在可逆导入导出前提下）。
3. 协作能力与高级 TM 维护能力（按产品需求再拆解）。

## 维护规则

1. 本文档只写“方向与阶段目标”，不写每天状态。
2. 每次里程碑调整时同步更新本文件。
3. 具体执行进度、阻塞、门禁结果统一更新 `DOCS/CURRENT_STATUS.md`。
